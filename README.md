# flask-smtplib-email
A minimal Flask application for sending emails using Python’s built-in smtplib library.

## Create and Activate a Virtual Environment (Optional but Recommended)

```bash
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
```
## Install Falsk

```bash
pip install Flask
```